# GeoClaw — full stack. Pinned to specific, known-good versions.
# Dependabot (see .github/dependabot.yml) keeps these up to date and raises
# a PR when a new upstream release is available; do NOT relax these to
# bare `>=` lower bounds — that allowed silent, untested upgrades in CI.
fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.3
psycopg2-binary==2.9.10
pandas==2.2.3
numpy==2.1.3
yfinance==0.2.50
requests==2.32.3
apscheduler==3.11.0
feedparser==6.0.11
google-generativeai==0.8.3
groq==0.13.0
openai==1.57.0
