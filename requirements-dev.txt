# Developer / CI-only dependencies. Installed by the lint and security
# jobs in .github/workflows/ci.yml and by pre-commit locally.
-r requirements.txt

ruff==0.15.12
bandit==1.9.4
pip-audit==2.10.0
pytest==9.0.3
pre-commit==4.6.0
